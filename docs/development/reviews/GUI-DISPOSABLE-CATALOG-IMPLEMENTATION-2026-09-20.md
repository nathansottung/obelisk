# Disposable catalog implementation - 2026-09-20

GUI_DISPOSABLE_CATALOG_READY_FOR_REVIEW

Author implementation and validation in the same Codex conversation as the prior GUI work; no independent reviewer/agent or owner acceptance of this new slice. ONE next action: substantive review of this uncommitted candidate.

Parent: `433cedac0a0f8a4e9dc67e3a1722b52c39c7cc6e`; branch `feat/gui-catalog-readonly`. The published two-commit chain and external acceptance receipt were read locally; no network lookup. Source checkpoint and exact submitted instruction are retained at `C:\Users\nsott\AppData\Local\ObeliskDev\gui-catalog-20260920-120412`.

## Contract recorded before implementation

Native representation: `catalog` in store.go, JSON schema_version 8. No second persisted format. A pure wrapper around the existing json.Unmarshal decoder will be shared without changing OpenStore semantics. OpenStore itself is excluded: it calls MkdirAll, may migrate/back up, seed profiles, repair/save and reconcile the jobs sidecar. The adapter constructs an isolated in-memory Store only after validation and reuses Store.Search for native relative-path substring and SHA-256 prefix queries. Search and its profile/location helpers are in-memory projections; no stored path is opened. Computed protection/parity values from Search will not be exposed as recorded evidence.

Bounded supported slice: current-schema collections, folders, current file records, chunks/copies, volumes and locations. Other populated sections will be refused in this initial reader rather than silently interpreted. Validate pointer entries, IDs, graph references, strings and resource bounds. Maximum input 4 MiB; 1000 files; bounded other tables and query lengths. Source-folder paths, package copy paths and labels are data only. File IDs identify native logical file records, not global deduplicated content; chunk membership must match the current recorded hash before showing a current-content occurrence. Retained versions and advanced sections are outside this slice.

Library will show recorded collections/storage and current file records; Find will query the same validated in-memory snapshot; inspector will show recorded relative/source-folder paths, SHA-256/algorithm/size and supported chunk-copy evidence. Live availability, capacities, parity counts and authoritative comparison reference are unavailable. Missing fields are labeled unavailable. No sample NAS/parity values are overlaid on catalog mode.

Boundary: an explicit early Go command branch, before production flag/default-directory/config/startup code, reads exactly one explicitly selected catalog file through a bounded read-only open. It emits an allowlisted display projection and accepts bounded JSON-line search requests on stdin. No HTTP backend or worker starts. Node's existing isolated server starts/stops this task reader and serves only intended assets and a bounded query response. Browser paths cannot select inputs. Catalog mode receives explicit success/failure bootstrap; failed input never falls back to demo. Input files live in a separate protected-input directory; fixture generation completes before readers start. Static preview launch and presentation remain supported. Logs/build caches/profiles/screenshots stay in task output directories.

Fixture setup will use native Go catalog types through a dedicated test generator, writing distinct valid A/B catalogs and valid-empty/refused controls in task-owned storage. Setup is separately recorded from read sessions. No config or keys are required. Source inspection, injected read-boundary failures, denied-write Store seams and input byte/directory inventories will establish the bounded read-only evidence; no platform-wide/ACL/power-loss guarantee is claimed.

## Implemented boundary and supported data

The contract above was written before source edits; the implemented refinements and limitations follow. [main.go:58](../../../main.go:58) recognizes only the explicit first argument `--gui-catalog-readonly` and returns before production flag defaults, configuration loading, OpenStore, App construction, workers or routes. The normal production startup branch is unchanged. [store.go:1159](../../../store.go:1159) extracts the exact existing json.Unmarshal into `decodeCatalogJSON`; openStore's classification, migration, initialization/save and jobs semantics remain in place. No new persisted contract is introduced.

[gui_catalog.go:27](../../../gui_catalog.go:27) is the input boundary: Lstat the explicit selected file, refuse nonregular/link input, os.Open read-only, Stat the opened file, bounded ReadAll and Close. No path from catalog contents reaches a filesystem function. [loadGUICatalog](../../../gui_catalog.go:50) discards bytes accompanied by any read error, shares the native decoder, requires schema 8, refuses missing/null native file sizes and validates the supported graph before constructing a private read-only Store. No directory creation, sidecar, lock, migration, profile seeding, repair, initialization, registration, source scan/hash, save, or jobs loading occurs in this reader. Store readOnly/failSave guards are defense in depth, not the reason the path is read-only.

Validation caps the input at 4 MiB, current files at 1000, ancillary collection/folder/chunk/volume/location tables at 100, potential membership-copy combinations at 1000, strings at 4096 bytes, and individual slices at 1000. IDs must be positive/unique within tables, rows nonnull, file/folder/collection and copy/volume/location references present, sizes nonnegative and recorded SHA-256 values valid. Populated advanced catalog sections, retained versions/events and spanning are refused. This deliberately limited reader is not a replacement validator for every native catalog. It retains native JSON decoder behavior for unknown members/duplicate JSON keys, and does not promise adversarial concurrent replacement/symlink-race or platform-wide enforcement. Inputs used here are task-owned synthetic regular files.

[runGUICatalog](../../../gui_catalog.go:276) emits an allowlisted projection, then processes JSON-line queries from stdin. It calls existing Store.Search with relative-path substring and SHA-256 prefix AND filters and a 1000-row limit. The Search caller chain (locationsMapLocked, chunkPhysCopies, fileProtectionLocked, resolveProfileLocked and related status helpers) was inspected: it locks and projects memory, without filesystem I/O/save. Only matched file IDs are returned; calculated protection/verified-copy totals are not exposed. Search's native retired-collection exclusion remains. Query protocol is bounded at 4096 bytes per line, 256 UTF-8 bytes of path text and 64 bytes of hash; startup/response timeout is five seconds. This small reader is single-query-at-a-time; concurrent requests may receive explicit 503, without crossing response slots.

Projection fields: collection ID/name/retired; volume ID/label/kind/recorded location; current file ID/collection/relative path/source-folder text/recorded byte size/hash algorithm/SHA-256/first-seen time; matched chunk name and copy ID/path/volume/location/superseded/recorded VerifyOK/LastVerifiedAt. Chunk membership must match current file ID and hash and collection before showing a current-content occurrence. No hash grouping, automatic deduplication, newest-file choice, authoritative reference or parity computation. Logical native file record identity, content checksum and physical-copy evidence remain distinct. Source-folder path is recorded context, not proof of a currently accessible source occurrence. Missing verification pointers and zero observation times are visibly unavailable.

Node's [catalog-adapter.mjs](../../../scripts/gui-preview/catalog-adapter.mjs) launches the explicit adapter executable without a shell, requires absolute launch paths and resolves the input's parent beneath local ObeliskDev. Only launch selects input; browser parameters cannot. The parent check does not certify that arbitrary files under ObeliskDev are synthetic: the operator must select these prepared fixtures. The child communicates over pipes, opens its catalog once, and is stopped/waited with the server. No production backend HTTP listener starts. The mode bootstrap is a display response, not a database; the only additional query route is `/catalog-query?text=...&hash=...`. It returns IDs or explicit error, with method/Host/query-key/length checks. The static route allowlist now includes catalog-ui.mjs and generated mode.mjs. Process bridge, tests, repository/design/evidence files and raw catalogs remain unserved. Static CSP remains connect-src none; catalog CSP permits same-origin requests only. No proxy, arbitrary file or operational route exists.

[catalog-ui.mjs](../../../scripts/gui-preview/catalog-ui.mjs) assigns strings via textContent. Library displays recorded collections/storage plus searchable records; Find uses the same snapshot and native query; selection uses the existing inspector/split/disclosure classes. This functional catalog-mode adaptation has no new Figma fidelity claim. Static Library/Smith Wedding remains the accepted presentation. In catalog mode, comparison counts/capacities/live accessibility/new verification and unshown workspace projections are unavailable; no Photography NAS/sample overlay. Operational controls are disabled. Mode buttons retain shell disclosure state without the static no-file-read demo message. No browser catalog picker/reload is implemented: stop/relaunch for a different input. Failed load displays a refused view with no static fallback; failed query clears selected results. Snapshot digest/session-load time are visibly separate from recorded observation dates.

## Fixture setup and exact launch

Evidence root **E**: `C:\Users\nsott\AppData\Local\ObeliskDev\gui-catalog-20260920-120412`. Inputs are exclusively `E\inputs`; build caches, temporary test directories, binaries, logs, screenshots and browser profiles are under `E\output`. Fixture generation completed before reader/browser execution, via TestGUICatalogGenerateFixtures using native catalog/Collection/Folder/File/Chunk/Copy/Volume types and json.Marshal. This is an explicit test-fixture write path, not a reader capability or a JavaScript copy of the schema. It refuses to overwrite an existing setup directory. No config or keys were needed.

Each ALPHA/BETA catalog contains one input-specific collection (including inert markup), two same-named files in different native folders with distinct hashes/sizes, two recorded volumes and two copy records for the first file only. The second file has no current-content copy membership. Their distinguishable persisted paths/names and selected details reach the browser. Empty is native schema 8 with no rows; malformed, zero-byte, future-schema and directory obstruction are explicit controls. Missing.json is deliberately absent.

| Input | SHA-256 |
|---|---|
| alpha.json | d934671f210312fc411fb58d7990d07de07f339ae6aa4264463dd7f462a0baad |
| beta.json | 7cd58c59ef7c06901ec92907046fb97afb402db16dd092f937c094d7c41d64f3 |
| empty.json | 687fceb562a745dd932b0d44515428b0a167bda95e1f63c08d191f026b99bf23 |
| malformed.json | be302461cda285db3ed2d45fb767b23e08ca8808b35a638009937df22b332d73 |
| zero.json | e3b0c44298fc1c149afbf4c8996fb92427ae41e4649b934ca495991b7852b855 |
| future.json | cef663127cc61a3bc43713745793b553f80257ddb894100a3bf0e7073c135b80 |

Selected installed tooling: Node v24.19.0 and Chrome 153.0.8010.48. Initial ambient Go was 1.27.0 with an empty default module cache; test/build setup failed with GOPROXY=off and no tests ran. Retained `go-initial.jsonl`/`build.txt` document that failure. The existing baseline record identified the already-installed Go 1.26.8 and approved module cache, which were reused without installing/downloading or switching platform. Process-scoped GOTOOLCHAIN=local, GOPROXY=off, GOFLAGS=-mod=readonly, CGO_ENABLED=0; GOCACHE/GOTMPDIR/TEMP/TMP point to E output. No global settings changed. Exact commands/environment are retained in `E\commands.md`; usage commands also appear in [README](../../../scripts/gui-preview/README.md).

Actual setup command, with installed Go path abbreviated **GO** as `C:\Users\nsott\AppData\Local\ObeliskDev\audit-2026-09-19\go-mod\golang.org\toolchain@v0.0.1-go1.26.8.windows-amd64\bin\go.exe`:

```powershell
$env:OBELISK_GUI_FIXTURE_OUTPUT = 'C:\Users\nsott\AppData\Local\ObeliskDev\gui-catalog-20260920-120412\inputs'
& $go test -json -run '^TestGUICatalogGenerateFixtures$' .
Remove-Item Env:OBELISK_GUI_FIXTURE_OUTPUT
& $go build -o "$task\output\obelisk-gui-reader.exe" .
node scripts/gui-preview/server.mjs 0 --catalog "$task\inputs\alpha.json" --adapter "$task\output\obelisk-gui-reader.exe"
```

Here `$task=E`, `$go=GO`; do not rerun setup into existing inputs. For a new fixture set use a new nonexistent output directory and `-count=1`. The already-prepared inputs above can be read directly. Static launch remains `node scripts/gui-preview/server.mjs 0`.

Expected catalog notice: **Disposable catalog mode — reading a synthetic catalog snapshot. No source/media files are opened. The selected catalog is not modified.** Open the new printed loopback URL; no old port is assumed. Stop with Ctrl+C and wait for the prompt, or type `stop` and Enter. Automation sends `stop\n`, closes stdin and waits for exit 0; its message records reader termination. Windows process.kill is abrupt and is not a graceful Ctrl+C test. Task-owned browser/direct-server harnesses close their reader explicitly and await exit. No owner process was stopped.

## Execution and outcomes (this task only)

| Execution | Actual outcome / retained log |
|---|---|
| Native fixture generation | 1 top-level pass, no fail/skip/subtests; fixture-setup.jsonl; explicit setup writes |
| Initial adapter tests with cached toolchain | 3 top-level passes, 12 subtest passes; go-adapter.jsonl; followed by successful build |
| First full selected Go compatibility run | 17 top-level passes, 18 subtest passes, 1 scale skip; go-final.jsonl |
| Final Go after explicit missing/null-size validation | 17 top-level passes, 20 subtest passes, 1 skip (TestCatalogScale), 0 failures; go-final-v2.jsonl |
| Final build/vet/read-only gofmt | all exits 0; no formatting paths; native-final-exits.json |
| Existing static Node suite | 7 top-level passes, 0 fail/skip/subtests; node-static.txt |
| Catalog Node initial run | 3 top-level passes, 1 failed Host probe, 0 skips; node-catalog.txt |
| Corrected Host probe only | 1 top-level pass; node-host-recheck.txt |
| Final catalog Node suite after request-slot guard | 4 top-level passes, 0 fail/skip/subtests; node-catalog-final.txt |
| Static CLI after shutdown changes | 1 top-level pass, 0 fail/skip/subtests; static-cli-final.txt |
| Static browser | 55 assertions pass; browser-static/ |
| ALPHA, BETA, fresh ALPHA restart | 22 assertions pass in each separately launched session; browser-alpha/, browser-beta/, browser-alpha-restart/ |
| Valid empty | 3 assertions pass; browser-empty/ |
| Malformed, zero, missing, future, directory input | 2 assertions pass per separate session; browser-malformed/, browser-zero/, browser-missing/, browser-future/, browser-directory/ |
| Final ALPHA on final native binary/bridge | 22 assertions pass; browser-alpha-final/ |

Overlapping runs are not summed. Native selected regex `^Test(GUICatalog(NativeDecodeAndSearch|RefusedLoads|ReadLifecycle)|OpenStore_|PersistObserver_|Catalog)` also selected the existing TestCatalogScale, which skipped for its opt-in requirement; no large-scale qualification is claimed. It includes the native open/read-refusal, initialization, recovery-save, newer-schema and migration round-trip compatibility controls for the unchanged production policy. No full Go suite/platform/cross-build campaign. Vet ran ./...; gofmt -l was read-only on the four changed Go files after formatting the two new files. Native exits are recorded explicitly.

The initial catalog Node failure was a test-boundary issue: fetch did not send the intended foreign Host. It was changed to raw http.get (as the existing isolation suite uses); the genuine foreign Host was refused. Later source inspection found that a busy query could write into a pending response slot; a pre-write busy guard and concurrent-request assertions fixed this before final handoff. Busy now returns explicit 503, with no response mixing and a subsequent successful native query. This is a bounded response-coherence check, not general concurrency qualification. Additional missing/null-size controls prevent fabricated zero-byte observations; their final Go run and final positive browser path passed. All attempts and reasons for repeats remain retained.

Browser checks use real catalog routes/handlers, source-specific names/paths/hashes, two duplicate-name records, copy selection/disclosure, no-results clearing, hash-prefix filtering and inert markup. Keyboard setup focuses first native navigation link, then actual Shift+Tab reaches visible skip, Enter focuses main, Tab reaches Guided and reverse navigation remains usable. Library, Find and Find-after-Expert-rerender preserve URL/history/timeOrigin/query/selected detail/open disclosure across skip. Real links and Back/Forward pass; the unchanged static 55-check suite covers Smith Wedding and its breadcrumb/disclosures. Catalog mode does not invent a Smith Wedding project comparison. All screenshots are 1440x1024 CSS/scale 2 where applicable; final ALPHA, BETA selected-detail and malformed-view images were inspected. They are functional adaptations, not newly supplied Figma designs.

## Read-only evidence, preservation and limits

The input boundary tests inject missing, permission and arbitrary I/O errors together with valid bytes and require refusal. They assert exactly one selected-path read on successful load; repeated native Search/projection invokes no persistence observer/failSave and leaves in-memory catalog bytes unchanged. Source/caller inspection shows the only input filesystem operations are read-only Lstat/Open/Stat/ReadAll/Close; production startup is bypassed. Native lifecycle tests execute two reader sessions plus missing/directory failures and compare names/types/bytes. Node checks separately inventory the prepared input directory before/after successful sessions, queries, restart, failures and shutdown. Final input directory has exactly six unchanged files and the original empty directory obstruction, with missing.json still absent. No state/lock/backup/sidecar was created. This combines positive source/boundary evidence with inventories rather than treating GET-only routing or final hashes alone as proof. Last-access metadata is not counted as a content write. No kernel-wide transient-write trace, ACL/power-loss guarantee, concurrent replacement qualification or real-media access is claimed.

Browser logs contain only the selected fresh loopback origin's explicit assets/mode/query routes, with no page runtime/console error or external/API request. Node negative routes refuse raw catalogs, adapter/test/source, Figma/ZIP/PDF, arbitrary paths, unsupported query keys, overlong/duplicate parameters and mutation methods; raw foreign Host is refused. Stored `/synthetic-never-open/...` paths remain strings; no stat/enumeration/hash/preview of those roots. Source strings are text, not links or markup. Native file hashing is only the selected catalog's input-identity digest, not source verification.

The initial branch/HEAD/index/operation check and local receipt established the exact published checkpoint; feat/gui-catalog-readonly was created once at it. No network operation. The original 254-path repository manifest is in before.json; retained pre-edit sources are in source-before/. Final candidate identities include new/untracked Go/JS tests and report, alongside changed paths in `candidate-identities.json`; final-preservation.json distinguishes authorized changes from preserved paths. No counts serve as a staging allowlist. The two narrow production-path changes are main's early command branch and the pure native decoder extraction; unrelated backend source is unchanged. Synthetic embedded fixtures, HTML/CSS baseline, dependency manifests, historical GUI/safety reports, Figma, ZIP/PDF/PNG references and previous raw evidence are preserved. All task-owned reader/server/browser processes are stopped/waited; process records live beside each browser run. Index remains empty; no commit, staging, push, merge, publication or automatic review.

Historical evidence remains separate: prior GUI author and closing reviewer each ran their own 7 Node/55 browser checks, and publication ran no new runtime tests. This task's results above are new author evidence. R1's previous closure is preserved, not borrowed as proof of this integration. No production/real inventory/recovery/ACL/platform/Docker/hardware qualification. PR-04, Unicode/tar, broader persistence/keystore/helper work remains separate. Generation-independent preservation/buffering, LTO-8 first physical qualification (not a generation cap), other backends/generations explicitly qualified and separate Blu-ray workflow remain unchanged. Stop for one substantive review.

## Final path-level handoff

Eleven existing paths changed: main.go, store.go; scripts/gui-preview/app.mjs, server.mjs, browser-check.mjs, preview.test.mjs and README.md; docs/development/CODEX_HANDOFF.md, NEXT_ACTIONS.md, OB_STATUS.md and REVIEW_COVERAGE.csv. Seven new paths: gui_catalog.go, gui_catalog_test.go; scripts/gui-preview/catalog-adapter.mjs, catalog-ui.mjs, catalog-browser-checks.mjs and catalog.test.mjs; this implementation report. The index is empty. These 18 delta paths are not a staging instruction.

Final preservation records 243 of the 254 starting paths byte-identical, exactly those 11 authorized existing changes, no missing files and seven added paths. All seven input entries (six files plus the empty directory obstruction) and seven extracted reference entries match; missing input remains absent. No task reader/Node/Chrome/Go process remains. `candidate-identities.json` covers the complete scoped candidate including unchanged runtime assets, and `final-current-identities.json` covers the full reconciled working path set. `candidate/` retains the scoped final source bytes for review/resumption; `source-before/` retains the selected starting source bytes. Raw logs/screenshots/binary hashes are in `evidence-identities.json`. No staging, commit, push or merge occurred.

# Bounded persistence review - 2026-09-19

BOUNDED PASS COMPLETE

Source: 0dc7d5399c6889e015aebc9ba02e694a909d6e9c
Branch: setup/windows-nsott

Scope: configuration defaults/load/save and startup/setup/settings callers; JSON keystore validation/sync/generation/lookup and callers; catalog/job reopen, recovery, backup and failure-reporting boundaries. Large files receive PARTIAL coverage with exact ranges. No full source audit or baseline rerun.

Starting checkout matches the handoff: NEXT_ACTIONS.md has the authorized baseline append; baseline/handoff and docs/Obelisk.fig are untracked and preserved; staging empty.

Planned sections: (1) tracked-file coverage inventory; (2) configuration persistence; (3) keystore persistence; (4) catalog/job reopen residuals; (5) bounded synthetic probes and final consolidation. Completed sections and precise evidence follow; this is the saved starting plan.

Current-machine baseline remains 209 top-level pass / 0 fail / 39 skip, separately 10 passing subtests; three hardware probes excluded. No broad runtime coverage is inferred. PR-04 candidate unavailable; Figma context pending. Neither blocks this review.

## 1. Configuration persistence - source section completed

OBX-004: CONFIRMED_AT_REVIEWED_COMMIT by source inspection; bounded execution pending. pipeline.go:38-186 defines defaults and load/save. LoadConfig suppresses read and JSON errors. Missing, unreadable, zero-length and syntactically malformed input can produce defaults without an error; type-invalid JSON can partially populate the defaults before Unmarshal reports an ignored error. Null leaves defaults; unknown fields are ignored. There is no config schema/version gate. SaveConfig merges over this possibly degraded value, then writes directly with os.WriteFile (0644): no temporary publication, fsync, previous-generation backup or rollback. Existing good known fields survive a valid partial update; unknown fields are not round-tripped. Incoming known-field type errors are returned before writing. Input marshal errors are ignored, but the subsequent Unmarshal of empty bytes returns an error; do not call this branch a demonstrated successful destructive write. Normal source-guard and write errors reach HTTP 400, then the Settings error toast.

Reachable chain: UI saveCfg (ui/index.html:3757-3784) / tool path save / UI-mode save -> PUT /api/config (main.go:740-747) -> body (314-321, decode errors ignored) -> SaveConfig -> LoadConfig -> direct publication. GET /api/config silently reports defaults/partial data after bad reads. POST /api/setup (753-771) -> ApplySetup (setup.go:239-272) -> applyGlobalIntegrity (integrity.go:173-181) -> SaveConfig, followed by another SaveConfig and best-effort Store.Log. Setup is two writes, not a single transaction; second-write failure can leave integrity changes applied. Setup normalizes vocabularies, mode and presets; generic SaveConfig does not perform those normalizations. Integrity normalizes effective verify settings at consumption (integrity.go:50-94). Raw negative/arbitrary values are not all rejected at save.

Impact includes lost storage/tool paths and persisted authentication token. Startup main.go:56-124 takes environment-token precedence and REFUSES non-localhost bind without a token; this review does not claim corruption bypasses that guard. authMiddleware captures the token at startup; changing config does not silently replace the live middleware token. A later restart can lose configured access or change localhost authentication. Baseline TestSettingsConfigFields and TestApplySetup_PreservesUnrelated cover healthy merges; TestAuthMiddleware covers the middleware, not corrupted config. Missing regression cases: unreadable/corrupt/unsupported config, partial decode, unknown-field preservation, failed publication preserving prior good bytes, and setup second-write failure.

Smallest proposed work: explicit fallible configuration load with intentional-first-use policy; reject malformed/unsupported persisted state before mutation; preserve prior bytes using checked atomic publication. Keep setup transaction handling explicit. Boundary checks remain lexical and need reconciliation with missing PR-04; no candidate behavior is assumed. No fixes applied. Next section: keystores.

## 2. Keystore persistence - source section completed

OB-006: CONFIRMED_AT_REVIEWED_COMMIT. pipeline.go:326-497 was read end to end. readStore distinguishes missing (returns a new empty store), I/O error, invalid marker/JSON and newer schema; SyncKeystores discards those errors, merges only readable stores, then writes EVERY configured path. A corrupt or newer-format but writable participant can therefore be replaced with a union that omits its keys. There is no prevalidation barrier, prior-generation retention or rollback. writeStore uses 0600 temporary bytes and os.Rename; it checks write/rename errors but neither syncs bytes nor retains a backup, and ignores JSON marshal errors (external JSON itself cannot contain NaN; no reachable NaN corruption claim made).

KeystoreStatus compares reference sets, not secret equality. Duplicate paths count toward MinKeystores; device diversity/identity is not validated. Missing participants count as reachable empty stores. Same-reference/different-secret stores can report OK; Passphrase chooses the first readable matching record while Sync chooses the last configured record. GenerateKey checks status, then rereads and writes sequentially; a later participant failure leaves earlier publications, returns an error and no reference, and never reaches metadata recording. When all key writes succeed, AddKeyMeta (store.go:3407-3412) drops save errors, so successful generation/API output need not have persisted catalog metadata. A later successful save might recover the in-memory metadata; a restart first loses it.

Reachability: main.go:2147-2181 exposes status, generate, sync and QR; UI vKeys (ui/index.html:2678-2698) directly POSTs sync and reports only count/error. BuildChunk calls GenerateKey (pipeline.go:1144) before encryption and key publication is independent of GPG execution. Recovery-kit Passphrase lookup (recoverykit.go:125-177) shares conflict precedence. No synthetic probe will require GPG/PAR2 or print a secret.

OB-011: source-observed confidentiality residual, not Windows ACL validation. Live keystore creation requests 0600; config requests 0644; recovery QR/sheet/HTML files request 0644 under 0755 directories. RestoreAppBackup writes keystore temporaries with 0644 (appbackup.go:388-425); include_keys exports plaintext keystore bytes and tar modes 0644 (129-141, 186-231). Mode requests do not establish effective POSIX permissions under arbitrary umask, nor Windows ACL protection. No actual secret exposure across accounts was tested. Export correctly clears AuthToken (111-117); restored blank token preserves the current config token (371, 430-435). No universal redaction claim is made.

Minimum proposed OB-006 change: validate all participants and key records/conflicts before any writes, distinguish enrollment from disappearance, and retain recoverable previous generations with explicit partial-publication outcomes. Generation metadata needs a checked persistence result; do not remove a key already used by media as rollback. Windows ACL policy is separate OB-011 work. PR-04 may overlap path identity/leaf containment but no key reconciliation repair is assumed. Existing paperkey/recovery tests exercise healthy JSON-key flows; missing coverage is the failure/conflict matrix above. Next: catalog/job reopen residuals.

## 3. Catalog/job reopen boundaries - source section completed

PR-01 bounded read rejection is FIXED_IN_PUBLISHED_SCOPE (store.go:1156-1247): non-not-found read errors, empty bytes and syntactically invalid JSON reject startup; newer catalog schemas latch catalog writes read-only. Missing storage/absent catalog still permits seeding: OB-001 remains PARTIALLY_FIXED overall. Structural validation is narrower than JSON parsing: null/empty objects and nil rows are not comprehensively rejected. NextID is initialized if nil but not reconstructed from catalog entity maxima; store.next (1576-1579) blindly increments. The jobs loader DOES raise its next counter to observed job IDs; do not report that same counter defect there. Catalog semantic/counter cases will be probed and retained as OB-001-related observations requiring broader-register reconciliation, not assigned speculative new IDs.

OBX-004 jobs residual: store.go:3530-3589 suppresses unreadable/zero/malformed sidecar load errors, leaving an empty board that a later NewJob (3596-3609) can publish over the prior file. Syntactically valid rows containing null can dereference nil in loadJobs. A missing jobs sidecar is legitimately optional, but corruption is not distinguished to callers. UI/API sees the resulting empty board; no new load-error field is surfaced. Valid RUNNING rows become INTERRUPTED and counters are recovered; failed startup reconciliation save is intentionally best-effort and re-derived at every reopen, not reopened as a PR-03 blocker.

PR-03 is FIXED_IN_PUBLISHED_SCOPE: saveJobs/writeJobsRows (store.go:3440-3524), NewJob, markJobUnrecordedLocked and FinishJob (3658-3722), and runJob/noteUnrecordedJob (main.go:503-614) preserve checked publication and separate execution outcome from current recording qualification/history. COMPLETED with unrecorded is allowed and truthful. Accepted current-versus-historical semantics stand. Catalog and jobs remain two files, Windows directory sync remains a no-op, and process logging is not a power-loss persistence guarantee. This pass does not repeat its complete concurrency review.

Best-effort backups: backupBeforeMigrate (1377-1384) ignores write errors despite a comment saying failures log; migration proceeds. dailyBackup (1546-1564) sets lastBak before attempted creation, ignores create/prune errors, and may prune even after failed creation. There is no same-day retry after failure in that Store instance. Startup recovery save failures are correctly logged (1306-1314), unlike these backup failures. Store.Log/AddKeyMeta/AddCollectionKind still discard save failures (1581-1586, 3407-3412, 1608-1626); these are residual OB-002 scope, not evidence that published job completion reverted.

App-state generation boundary (OB-010, PARTIALLY_FIXED by safe single-file replacement only): gatherMembers (appbackup.go:100-143) checks catalog/key reads but silently omits unreadable optional jobs/formats and serializes LoadConfig defaults on config failure. It reads separate live files without a generation snapshot. Export (149-245) publishes tar, then sidecar separately; main startup ignores maybeAutoExport errors (main.go:83-88), while maybeAutoExport (506-526) treats an existing tar as sufficient for the period. Restore (363-455) best-effort copies current records, publishes members sequentially, parses config late and only then reopens/swaps Store. Errors can leave a mixed generation. PR-02 atomicRename preserves an existing destination on failed publication; no destructive fallback was reintroduced. Full archive namespace/concurrency review is deferred. Proposed regressions: failed backup creation with successful primary save; corrupt optional records in export; null jobs; catalog missing/stale counter; restore late failure retaining coherent generation. PR-04 may overlap ownership/path boundaries but does not establish any repair here. Next: bounded synthetic probes, then ledger consolidation.

## 4. Executed evidence and limits

One new command, no full-suite rerun: C:\Program Files\Go\bin\go.exe test -mod=readonly -count=1 -json -run ^TestPersistenceReview_ -timeout 2m .

Working directory: C:\Users\nsott\AppData\Local\ObeliskDev\persistence-20260919-131458-90213423\source. Process-scoped GOTOOLCHAIN=go1.26.8, CGO_ENABLED=0, GOWORK=off, GOFLAGS=-mod=readonly, isolated TEMP/TMP/GNUPGHOME; no GOTMPDIR override. Exact settings, timestamps, native PID and exit are retained in C:\Users\nsott\AppData\Local\ObeliskDev\persistence-20260919-131458-90213423\probe-execution.json. Native exit 0, terminal package PASS; 13 top-level PASS / 0 FAIL / 0 SKIP, separately 5 subtest PASS. PASS means the observation and controls matched, including reproduced unsafe behavior; it does not mean these issues are fixed.

The copy was reconstructed from all 208 pinned Git blobs, with Git blob IDs and SHA-256 in source-identities.json. No production seam was added: probes use real functions, synthetic files and existing Store.failSave. Exact probe source remains at C:\Users\nsott\AppData\Local\ObeliskDev\persistence-20260919-131458-90213423\source\persistence_review_probe_test.go; raw JSON/logs under C:\Users\nsott\AppData\Local\ObeliskDev\persistence-20260919-131458-90213423\logs. No secret values were printed. Repository test files were not changed.

| Probe (TestPersistenceReview_ prefix) | Observed result / controls |
|---|---|
| ConfigHealthyAndInvalid | Healthy partial merge and invalid incoming type preserve prior bytes; five persisted-input cases (empty/malformed/null/wrong-type/unknown-schema) are silently rewritten. |
| ConfigUnreadableAndMissing | Missing defaults are allowed; a directory at config.json is treated as defaults; write returns an error and a retained original remains intact. This is a real read-error control, not a transient sharing-error reproduction. |
| ConfigHTTP | Malformed stored config plus malformed HTTP PUT body returns 200 and publishes defaults through the real mux; this is handler execution, not a network/auth bypass. |
| KeystoreHealthyAndRejectedParticipant | Healthy sync control succeeds; directly rejected malformed and newer-schema participants are subsequently overwritten by SyncKeystores. |
| KeystoreConflict | Reference-only status reports OK; first-match Passphrase result changes after last-wins sync. Secret values never logged. |
| GeneratePartialPublication | Both participants start valid; a directory at the second .tmp makes generation fail after first publication; second prior bytes survive. Removing the obstacle lets sync recover. |
| GenerateMetadataFailure | Catalog failSave seam preserves prior disk bytes but GenerateKey returns success; metadata is absent after reopen. Clearing the fault and recording metadata is a positive control. |
| DuplicateMissingParticipants | Two duplicate missing paths satisfy MinKeystores; generation yields two records in one file, not independent redundancy. |
| JobsReopen | Valid RUNNING reopen -> INTERRUPTED control; zero/malformed/null jobs input yields empty board and next NewJob replaces it. |
| NullJobRow | Valid JSON rows:[null] causes a recovered panic inside OpenStore/loadJobs. The probe catches it locally; no production process was crashed. |
| CatalogMissingAndNull | Retained known-good catalog stays byte-identical while absent original initializes anew; literal null is accepted and overwritten by seeding. |
| CatalogCounter | A valid catalog containing an existing collection but empty NextID reopens; next collection reuses the ID. No assertion that normal successful saves spontaneously corrupt counters. |
| BackupFailure | An obstructing directory at the daily backup name plus a cleared lastBak allows primary save, latches the day, and suppresses retry after obstruction removal. Clearing lastBak is the successful backup control. This probes obstruction/no-retry, not injected WriteFile failure. |

Evidence not executed: interrupted direct config write/power loss; pre-schema backup WriteFile failure; genuine transient unreadable-keystore-then-writable condition (malformed/newer-schema exercised instead); ACL enforcement or POSIX umask behavior; setup second-write fault; coherent multi-file restore failure; concurrency/path-alias races. These remain source-observed or NEEDS_INVESTIGATION. Existing PR-01/02/03 baseline test outcomes are reused, not represented as newly run or complete re-review. Catalog null/counter observations belong to further structural-validation work and need reconciliation against unavailable broader OB-014..OB-036 scope before assigning any new ID; no ID invented.

## 5. Findings disposition and recommendation

| Issue | Current classification | Remaining work |
|---|---|---|
| OBX-004 | CONFIRMED_AT_REVIEWED_COMMIT | Fallible config/job load, schema/shape validation, checked config publication; regression cases retained above. Null-job row belongs to the same unvalidated-load boundary. |
| OB-006 | CONFIRMED_AT_REVIEWED_COMMIT | Validate every participant before sync writes; detect conflicting secrets; explicit enrollment/identity; recover partial publications and retain key generations. |
| OB-011 | CONFIRMED_AT_REVIEWED_COMMIT (source policy only) | Define and test Windows ACL and POSIX creation/replacement policy for all secret-bearing artifacts. No cross-account disclosure established here. |
| OB-001 | PARTIALLY_FIXED; PR-01 FIXED_IN_PUBLISHED_SCOPE | Intent/identity of missing catalog remains unresolved; semantic shape/counter observations need broader-register reconciliation. |
| OB-002 | PARTIALLY_FIXED; PR-03 FIXED_IN_PUBLISHED_SCOPE | Legacy metadata/mutator persistence errors still dropped; best-effort backup/audit outputs are not durable guarantees. |
| OB-003 | FIXED_IN_PUBLISHED_SCOPE | Safe single-file replacement stands; not a cross-file transaction or power-loss guarantee. |
| OB-010 | PARTIALLY_FIXED only for shared safe replacement | Backup/restore still lacks coherent generation publication; this pass does not certify archive namespace safety. |
| OB-004/OB-007 | NOT_REVIEWED as whole issues | Lexical source guard and publication ownership are overlap points to reconcile with missing PR-04. |

ONE recommended next action: prepare the bounded OB-006 validation-before-write repair with conflict/refused-participant regression tests. It is the directly reproduced path that overwrites rejected key stores and can make otherwise healthy encrypted media unrecoverable; waiting for PR-04 must not be mistaken for protection here. Any implementation needs a subsequent authorized task. Do not start a full framework/concurrency rewrite. Prior-generation and partial-publication policy must remain explicit residual scope if not included in that bounded repair.

## 6. Coverage inventory and handoff

208 tracked rows plus 3 separately classified local artifacts = 211 ledger rows. Tracked classifications: {"binary/embedded artifact": 1, "build/CI/configuration": 11, "data/configuration": 4, "embedded artifact metadata": 1, "first-party frontend": 1, "first-party production": 75, "first-party test": 62, "historical reference": 21, "reference/documentation": 24, "test fixture": 8}. Source-depth totals: {"EXCLUDED_WITH_REASON": 47, "INVENTORIED_ONLY": 144, "PARTIAL": 13, "SOURCE_REVIEWED": 4}. SOURCE_REVIEWED means only the entire named file was read substantively; PARTIAL contains exact symbols/ranges. Reading a helper file is not comprehensive safety validation.

Four entire files reviewed: setup.go, dirsync_windows.go, auth_test.go, persistence_test.go. Thirteen mixed files partially reviewed, listed with exact ranges in REVIEW_COVERAGE.csv. Others remain inventoried or explicitly excluded. Test-file rows attach actual named baseline outcomes; they never transfer package PASS to all production files. Only pipeline.go/store.go/main.go have this pass's bounded runtime probes; no full-file runtime status is claimed. No vendored tree or generated Go marker was found in tracked source. The embedded escrow tarball is binary material, not source-reviewed; its accompanying placeholder metadata is explicitly excluded. Historical review/reference docs and local Figma are not application source. Current review documents and local synthetic probes are review evidence, not newly implemented product code.

Remaining review: rest of store/pipeline/main; application backup namespace and generation restore; source/restore/write coverage, scheduler/plans, archive/tape/optical, helpers, frontend and feature matrix. Figma frame access and PR-04 transfer remain pending separately. No substantive earlier subagent report is assumed. Baseline was not rerun.

Final preservation check: all production/test/build/dependency files and Figma bytes match starting SHA-256 identities; baseline unchanged. All 208 copied pinned source files remain byte-identical. Only the authorized NEXT_ACTIONS append changed an originally tracked workspace file; its prior bytes were verified intact. Branch/HEAD/staging unchanged. Current handoff/review/ledger updated; no historical baseline/review changed. No fixes implemented. Verification initially compared Windows path separators to a slash literal; normalized path comparison passed without changing product files.

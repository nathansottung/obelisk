# Optional developer-alpha report

- Package ID and external ZIP SHA-256:
- Windows version/build/architecture; Node and browser versions:
- Action and expected result:
- Actual result, exit code and `published` value if applicable:
- Did the reader/launcher stop and did its loopback URL stop responding?
- Optional short, redacted log excerpt or screenshot:

Do not attach private catalogs, personal absolute paths, source photos, keys or credentials. Replace user/workspace names with placeholders. No automatic telemetry or upload occurs. Report any security detection or permission block with the exact displayed status/time and a redacted affected item; do not bypass it or assume an unsigned warning is harmless.
# Multi-snapshot/comparison context

For this local candidate, include the package ID/ZIP hash, one- or two-input mode,
startup order, chosen reference, OFF/ON/UNKNOWN scope and the observed recorded
class/count. State whether the issue survives stop/wait/reopen of the same
generated snapshots. Redact personal paths and identifiers; do not attach private
catalogs, source files or keys. No automatic upload is performed.
